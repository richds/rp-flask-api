pipeline {
    agent { dockerfile true }
    stages {
        stage('buildapp') {
            steps {
                sh 'python3 --version'
            }
        }
        stage('test'){
            steps {
                sh 'python3 --version'
            }
        }
    }
}